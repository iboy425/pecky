# Chin Tuck 智能动作识别产品执行计划

> 版本：v1.1  
> 日期：2026-08-27  
> 当前阶段：先跑通桌面原型，不决定最终耳夹、挂耳或披肩外形  
> 项目范围：只做 chin tuck 动作识别，不把 Lumia 2 的心率、血流、睡眠功能混进来。

---

## 0. 一页结论

我们当前要证明的闭环是：

~~~text
佩戴传感器
→ 自动采集动作
→ 区分 chin tuck 和干扰动作
→ 完整动作只计数一次
→ 串口/蓝牙发送结果
→ 软件界面自动显示
~~~

现有硬件足够完成黑客松级的单传感器原型：

- ESP32-S3-N16R8：主控、USB 串口、Wi-Fi、BLE。
- MPU6050/MPU6500 兼容六轴 IMU：三轴加速度和三轴角速度，是当前动作识别的核心传感器。现有蓝色模块实测为 MPU6500-compatible。
- 面包板和杜邦线：只用于验证接线。
- 压力传感器、黑色光学心率模块和电池：第一阶段全部不接。

必须诚实说明：

- 一个 MPU6050/MPU6500 兼容 IMU 可以跑通采集、分类、自动计数和数据传输。
- 一个单 IMU 无法保证在所有人和所有佩戴位置上完美区分 chin tuck、低头和看手机。
- 如果后续单传感器准确率不足，最有效的升级是第二个 IMU：一个跟随头部，一个固定在胸口/肩部，使用相对运动判断。
- 当前不需要画 PCB，也不需要定制小模块。
- IMU 模块排针必须焊接；不能用手压着排针或让焊盘虚接。
- 先用 USB 供电和串口，不接电池。功能通过后再做蓝牙和可穿戴包装。

### 0.1 2026-08-27 实测硬件验证结果

| 验证项 | 实测结果 |
|---|---|
| USB 串口 | COM7，USB-Enhanced-SERIAL CH343 |
| 主控 | ESP32-S3，固件编译、烧录和串口输出正常 |
| I2C 地址 | 稳定发现 `0x68` |
| 芯片身份 | `WHO_AM_I = 0x70`，因此按 MPU6500-compatible 处理，而不是在软件中严格假定为 MPU6050 |
| 六轴数据 | 加速度、角速度和温度连续输出正常 |
| 实际采样率 | `100.0 Hz` |
| 验证数据量 | `1175` 行有效数据 |
| 阶段结论 | ESP32-S3 → I2C IMU → USB 串口 → Windows 监视脚本的硬件数据链路已经通过 |

命名说明：本文后续出现的“MPU6050 模块”主要指现有蓝色开发模块的外形/排针名称；涉及驱动、身份码和寄存器时，一律按“MPU6050/MPU6500 兼容 IMU”处理。当前 Demo 使用兼容寄存器读取，不依赖只接受 MPU6050 身份码的专用库。

---

## 1. 产品目标和验收定义

系统必须区分以下动作：

| 标签 | 动作 |
|---|---|
| idle | 静止、正常坐姿 |
| chin_tuck | 标准下巴内收 |
| look_down | 低头 |
| look_up | 抬头 |
| nod | 点头 |
| turn_left | 左转头 |
| turn_right | 右转头 |
| shrug | 耸肩 |
| walk | 走路、身体震动 |
| drink | 喝水 |
| phone | 看手机 |
| posture_adjust | 正常坐姿调整 |

只有同时满足下面条件才算 1 次 chin tuck：

1. 动作从中立位开始。
2. 识别结果是 chin_tuck。
3. 置信度超过阈值。
4. 动作持续时间合理。
5. 动作回到中立位。
6. 当前不在防重复计数的冷却期。
7. 没有被走路强震动、耸肩等排除规则否决。

原型完成标准：

- 上电后自动开始，不需要点 Start。
- chin tuck 自动计数。
- 主要干扰动作不计数。
- 结果自动显示在电脑或手机端。
- 连续运行 30 分钟不崩溃。
- 有真实测试数据、准确率和误判记录，不只是偶然成功一次。

---

## 2. 当前硬件清单和职责

| 硬件 | 作用 | 当前是否接入 |
|---|---|---:|
| ESP32-S3-N16R8 开发板 | 主控、数据处理、USB 串口、BLE | 是 |
| 蓝色 MPU6050 外形模块（实测 MPU6500-compatible） | 六轴动作采集 | 是，已验证 |
| 面包板 | 固定传感器、辅助接线 | 可选 |
| 杜邦线 | 连接 ESP32 和兼容 IMU | 是 |
| RFP-602 薄膜压力传感器 | 可选辅助信号/标注 | 暂不接 |
| 黑色 HW-605 光学模块 | 心率/血氧类，不服务当前动作识别 | 不接 |
| 电池盒/电池 | 脱离电脑运行 | 暂不接 |

最小系统：

~~~text
Windows 电脑
    │ USB 数据线
    ▼
ESP32-S3
    │ I2C：3V3、GND、SDA、SCL
    ▼
MPU6050/MPU6500 兼容 IMU
~~~

不要把 MAX30102 类光学模块误当成第二个动作传感器；它不能提供头部姿态。

---

## 3. 总体技术架构

~~~mermaid
flowchart LR
    A[头部/耳侧兼容 IMU] -->|I2C 100Hz| B[ESP32-S3]
    B --> C[校准与滤波]
    C --> D[滑动窗口与特征]
    D --> E[动作分类器]
    E --> F[事件状态机与防重复计数]
    F --> G1[USB 串口]
    F --> G2[BLE Notify]
    G1 --> H1[Windows 采集/训练/调试]
    G2 --> H2[手机或电脑展示]
    H1 --> I[模型参数]
    I --> E
~~~

软件服务硬件的方式分两步：

### 研发模式

ESP32 只负责稳定采样，把原始数据通过 USB 串口发给电脑。电脑负责：

- 保存 CSV。
- 标注动作。
- 画曲线。
- 清洗数据。
- 训练模型。
- 计算精确率、召回率和混淆矩阵。
- 调整阈值和状态机。

### 演示/产品模式

把已经验证的滤波、特征和分类参数部署到 ESP32。ESP32 本地识别，只发送：

- 识别事件。
- 累计次数。
- 置信度。
- 电量和连接状态（后续）。

不要在 ESP32 上训练模型；ESP32 负责执行模型，训练放在电脑上。

---

## 4. 软件清单

### 4.1 必装软件

| 软件 | 用途 |
|---|---|
| Arduino IDE 2.x | 编译、烧录、串口监视和串口绘图 |
| esp32 by Espressif Systems | Arduino IDE 的 ESP32-S3 开发板支持 |
| Arduino `Wire`（随 ESP32 板包安装） | 当前实测 Demo 直接读取兼容寄存器，支持现有 `WHO_AM_I=0x70` 模块，无需额外 IMU 库 |
| Adafruit MPU6050 / Unified Sensor / BusIO | 仅当模块身份和库兼容性确认后作为可选方案，不是当前硬件验证的必装项 |
| Python 3.11 或 3.12（Windows） | 数据采集、训练、实时识别 |
| VS Code | 管理固件、Python 和数据文件 |

### 4.2 Python 包

~~~powershell
py -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install pyserial numpy pandas scipy scikit-learn matplotlib streamlit joblib bleak
~~~

说明：

- 串口是 Windows 的 COM 端口，所以采集脚本使用 Windows Python。
- 不建议在 WSL 里直接操作 COM7。
- Streamlit 用来快速做黑客松演示界面。
- bleak 用于后续 BLE，不阻塞第一版。

### 4.3 仿真软件的定位

Stack Anyway：

- 可以做模块选择、摆放、接线规划、BOM 和代码辅助。
- 可以用来展示结构方案。
- 不能代替真实焊接、供电、I2C、串口和佩戴动作测试。

Wokwi：

- 可以检查部分 ESP32 代码和支持的虚拟器件。
- 如果没有完全一致的 ESP32-S3 板和 MPU6050/MPU6500 兼容 IMU 模型，不要在上面浪费时间。
- 无法模拟真实佩戴松动、走路震动、人体动作差异和传感器噪声。

本项目主路径是实物验证；仿真只作为辅助。

---

## 5. Arduino IDE 配置

1. 安装 Arduino IDE 2.x。
2. 打开 File > Preferences。
3. 在 Additional Boards Manager URLs 填入：

~~~text
https://espressif.github.io/arduino-esp32/package_esp32_index.json
~~~

如果在中国大陆下载很慢，使用 Espressif 官方文档提供的镜像：

~~~text
https://jihulab.com/esp-mirror/espressif/arduino-esp32/-/raw/gh-pages/package_esp32_index_cn.json
~~~

4. 打开 Tools > Board > Boards Manager。
5. 搜索 esp32，安装 esp32 by Espressif Systems。
6. 选择 Tools > Board > esp32 > ESP32S3 Dev Module。
7. 开发板标记为 N16R8，可设置：
   - Flash Size：16MB。
   - PSRAM：OPI PSRAM。
   - 其他选项第一版保持默认。
8. 将数据线插入开发板标有 COM 的 USB 口。
9. 选择正确端口。之前照片显示 COM7，但重新插拔后编号可能改变。
10. 串口波特率统一为 115200。

串口注意事项：

- Arduino Serial Monitor 和 PuTTY 不能同时打开同一个 COM 口。
- 使用 Arduino IDE 前先关闭 PuTTY。
- PuTTY 循环显示 example: log -> UART，说明驱动和串口已经工作，但板子运行的是旧示例固件。
- 如果上传失败，按住 BOOT，点击上传，在 IDE 出现 Connecting 后松开 BOOT。
- 如果端口消失，换一根确认能传数据的 USB 线，不要使用只充电线。

---

## 6. 焊接和接线方案

### 6.1 IMU 模块必须焊接

把 1×8 直排针焊到以下 8 个孔：

~~~text
VCC  GND  SCL  SDA  XDA  XCL  AD0  INT
~~~

焊接验收：

- 每个焊点完整、发亮、牢固。
- 相邻焊点没有连锡。
- 排针垂直。
- 手轻轻晃动排针时焊点不动。
- 焊接后先断电目视检查。

不能把未焊排针临时插在孔里使用；这样非常容易产生虚接。

### 6.2 推荐：不使用面包板，直接接线

ESP32-S3 开发板太宽，不必强行塞进小面包板。按丝印直接连线：

| ESP32-S3 | 兼容 IMU | 作用 |
|---|---|---|
| 3V3 | VCC | 3.3V 供电 |
| GND | GND | 共地 |
| GPIO9 或丝印 9 | SCL | I2C 时钟 |
| GPIO8 或丝印 8 | SDA | I2C 数据 |
| GND | AD0 | 固定地址为 0x68 |

暂时不接：XDA、XCL、INT。

~~~text
ESP32-S3                      MPU6050/MPU6500 兼容 IMU

3V3   --------------------->  VCC
GND   --------------------->  GND
GPIO9 --------------------->  SCL
GPIO8 --------------------->  SDA
GND   --------------------->  AD0

                               XDA  不接
                               XCL  不接
                               INT  不接
~~~

插线规则：

- 断开 USB 后再插线。
- 按板上印刷的 3V3、GND、8、9 找孔，不凭物理顺序猜。
- VCC 使用 3V3，不接 5V/VIN。
- 接好后拍一张能同时看清两个模块丝印和所有线的照片，再通电。

### 6.3 面包板坐标示例

如果要用面包板，只把焊好排针的兼容 IMU 模块插上去。

让八个针脚分别插入：

| IMU 模块针脚 | 面包板坐标 |
|---|---|
| VCC | E5 |
| GND | E6 |
| SCL | E7 |
| SDA | E8 |
| XDA | E9 |
| XCL | E10 |
| AD0 | E11 |
| INT | E12 |

从同一行另一侧接线：

| 面包板坐标 | 接到 ESP32 |
|---|---|
| A5 | 3V3 |
| A6 | GND |
| A7 | GPIO9 |
| A8 | GPIO8 |
| A11 | GND |

同一编号行的 A、B、C、D、E 内部相连，所以模块板身悬在面包板外侧可以接受；关键是金属针必须真正插入孔内。

### 6.4 通电前检查表

- [x] IMU 模块排针已焊接（2026-08-27 实测）。
- [ ] VCC 接 3V3。
- [ ] GND 接 GND。
- [ ] SDA 接 GPIO8。
- [ ] SCL 接 GPIO9。
- [ ] AD0 接 GND。
- [ ] XDA、XCL、INT 没有乱接。
- [ ] 没有裸露金属短路。
- [ ] PuTTY 已关闭。
- [ ] 只接 USB，不接电池。

---

## 7. 项目目录

~~~text
firmware/
  01_serial_hello/
  02_i2c_scanner/
  03_mpu6050_stream/
  04_feature_debug/
  05_recognition_usb/
  06_recognition_ble/

tools/
  record_serial.py
  label_session.py
  plot_session.py
  build_dataset.py
  train_model.py
  export_model.py

dashboard/
  app.py

data/
  raw/
  processed/

models/
docs/
~~~

职责：

- 01_serial_hello：只验证 ESP32、USB 和串口。
- 02_i2c_scanner：只验证兼容 IMU 的 I2C 地址 0x68。
- 03_mpu6050_stream：历史目录名保留；实际按 MPU6050/MPU6500 兼容寄存器以 100 Hz 输出六轴 CSV。
- 04_feature_debug：滤波、姿态和特征。
- 05_recognition_usb：本地识别、USB 输出事件。
- 06_recognition_ble：本地识别、BLE 输出事件。
- tools：电脑端采集、训练和导出。
- dashboard：实时演示界面。
- data/raw：原始数据只追加，不手工修改。

---

## 8. 分阶段实施

### 阶段 0：ESP32 串口验证

工作：

1. 安装 Arduino IDE 和 ESP32 开发板包。
2. 数据线插入 COM 口。
3. 关闭 PuTTY。
4. 选择 ESP32S3 Dev Module 和正确 COM 口。
5. 上传最小程序，每秒打印：

~~~text
ESP32_READY 1
ESP32_READY 2
ESP32_READY 3
~~~

验收：

- [ ] 编译成功。
- [ ] 上传成功。
- [ ] Serial Monitor 在 115200 持续显示。
- [ ] 按 RST 后计数重新开始。

阶段 0 没通过时，不接 IMU 模块。

### 阶段 1：I2C 扫描

工作：

1. IMU 模块排针焊好。
2. 按五线方案接线。
3. 上传 I2C Scanner。
4. 查看串口。

期望结果：

~~~text
I2C device found at address 0x68
~~~

验收：

- [ ] 连续重启 5 次，每次发现 0x68。
- [ ] 轻碰导线不会丢失地址。
- [ ] 两块板都不异常发热。

失败排查顺序：

1. 立即断电。
2. 查 VCC 和 GND。
3. 查 SDA/SCL 是否接反。
4. 查焊点。
5. 查 AD0 是否接地。
6. 最后才改代码。

不要同时改接线和代码，否则无法知道问题来源。

### 阶段 2：读取六轴数据

工作：

1. 优先运行项目中的兼容 Demo，使用 Arduino `Wire` 直接读取寄存器，不额外安装专用 IMU 库。
2. 读取 `WHO_AM_I`：常见 MPU6050 为 `0x68`，当前模块实测为 `0x70`，按 MPU6500-compatible 处理。
3. 确认 ax、ay、az、gx、gy、gz 随动作变化。
4. 固定采样率为 100 Hz。
5. 输出一行一条 CSV。

数据格式：

~~~text
t_ms,ax,ay,az,gx,gy,gz
1250,-0.13,0.22,9.71,0.01,-0.08,0.02
~~~

验收：

- [x] 硬件链路连续输出正常，本轮获得 1175 行有效数据。
- [x] 实际采样率 100.0 Hz，位于 95–105 Hz 范围。
- [x] I2C 地址 0x68、`WHO_AM_I=0x70`，身份与读取方案已确认。
- [x] 转动模块时六轴数据产生对应变化。
- [x] 串口监视脚本验证通过，无大量缺行。

### 阶段 3：临时佩戴

当前不做最终耳夹，只验证传感器位置。

推荐：

- 兼容 IMU 固定在眼镜腿、发箍、耳挂或耳侧胶带上，必须随头部一起运动。
- ESP32 放在衣袋、肩部小袋或桌面。
- 杜邦线沿衣领布置，并用胶带做应力释放。

不要：

- 把整块 ESP32 挂在耳朵上。
- 让 IMU 在衣服内晃动。
- 把 IMU 只放在披肩中央却期待它精确测量头部动作。

验收：

- [ ] 连续佩戴 10 分钟位置不变化。
- [ ] 重复同一动作时曲线形态相似。
- [ ] 走路时导线不会把传感器拉歪。

### 阶段 4：数据采集

最低黑客松数据量：

- 3 名参与者。
- 每人每类至少 15 次。
- chin_tuck、look_down、phone 每人至少 30 次。
- 动作前后各保留 2 秒中立位。
- 每人至少分两个时间段采集。

推荐数据量：

- 5–10 人。
- 每类每人 30–50 次。
- 包含快慢、幅度大小、坐姿和站姿变化。
- 按人划分训练集和测试集，不能把同一个人的相邻片段随机分到两边。

CSV 字段：

~~~text
session_id,user_id,label,t_ms,ax,ay,az,gx,gy,gz
~~~

验收：

- [ ] 每个文件能追溯用户、动作和时间。
- [ ] 没有大量丢行或时间倒退。
- [ ] 原始文件备份且不手改。
- [ ] 保存失败动作和干扰动作，不只保存成功样本。

### 阶段 5：动作识别

第一版先做可解释基线：

1. 开机静止校准 3 秒。
2. 低通滤波，抑制高频震动。
3. 估计 pitch/roll 和角速度。
4. 使用 1.5 秒滑动窗口。
5. 每 0.2 秒更新一次。
6. 建立完整动作状态机：

~~~text
neutral
→ movement_started
→ chin_retracted
→ short_hold
→ returned_to_neutral
→ count +1
→ cooldown
~~~

特征包括：

- 六轴均值、标准差、最小、最大、峰峰值。
- 加速度和角速度 RMS/能量。
- pitch 变化、峰值、持续时间、回中立时间。
- 各轴相关性。
- 高频能量，用于排除走路震动。

分类器：

- 第一版使用 scikit-learn 的 RandomForestClassifier。
- 训练和评估在电脑完成。
- 先在电脑实时推理。
- 达标后导出简化规则或树模型到 ESP32。

计数条件：

1. 分类结果为 chin_tuck。
2. 置信度建议不低于 0.80。
3. 动作从中立位开始并返回。
4. 持续时间约 0.5–3 秒。
5. 不属于强震动。
6. 不在 1–1.5 秒冷却期。

目标指标：

| 指标 | 黑客松最低 | 后续产品 |
|---|---:|---:|
| chin tuck 召回率 | ≥85% | ≥95% |
| chin tuck 精确率 | ≥85% | ≥95% |
| 静止误计数 | 10 分钟 ≤1 | 60 分钟 ≤1 |
| 单次识别延迟 | ≤1.5 秒 | ≤0.8 秒 |
| 连续运行 | ≥30 分钟 | ≥8 小时 |

### 阶段 6：展示软件

必做版：USB + Streamlit。

ESP32 识别后输出：

~~~json
{"ts":1724720000,"event":"chin_tuck","count":12,"confidence":0.91}
~~~

界面显示：

- 今日次数。
- 最近动作。
- 置信度。
- 实时六轴曲线。
- 当前分类。
- 误判标记按钮。

这是黑客松最稳的展示路径。即使 BLE 没完成，也能完整证明真实动作采集、自动识别和自动计数。

第二版：BLE。

1. ESP32 上电后自动广播。
2. 展示端连接并订阅 Notify。
3. ESP32 发送事件和次数，不连续发送全部原始数据。
4. 断线时 ESP32 本地继续计数。
5. 重连后同步累计值。

验收：

- [ ] 上电后无需 Start。
- [ ] chin tuck 后界面自动加一。
- [ ] 干扰动作不加一。
- [ ] 展示端断开时 ESP32 仍识别。
- [ ] 重连后次数不丢。

### 阶段 7：外形决策

只有以下全部通过后才选耳夹、挂耳或披肩：

- 接线和采样稳定。
- 找到传感器最佳固定位置。
- 识别达到最低指标。
- 明确是否需要第二个 IMU。
- 明确续航和电池需求。

| 形态 | 优点 | 风险 | 当前建议 |
|---|---|---|---|
| 耳夹 | 隐蔽、像首饰 | 现有模块放不下，重量和夹痛明显 | 后续小型化阶段 |
| 挂耳 | 重量可放耳后，空间较大 | 仍需电池和外壳 | 最值得优先产品化 |
| 智能披肩 | 主控、电池好隐藏 | 中央传感器不代表头部 | 主控可放肩部，头部仍要传感器 |
| 发箍/眼镜腿 | 传感器固定好 | 不是最终外观 | 当前验证最佳 |

现阶段允许主控和电池放在口袋/肩部，只把兼容 IMU 放到头部。不要要求整套开发板进入耳夹。

---

## 9. 可选的第二 IMU

如果单个 MPU6050/MPU6500 兼容 IMU 不能稳定区分耸肩、走路、看手机和 chin tuck，后续增加同型号、身份码已确认的第二 IMU：

- 头部 IMU：AD0 接 GND，地址 0x68。
- 躯干 IMU：AD0 接 3V3，地址 0x69。
- 两者共用 3V3、GND、GPIO8/SDA、GPIO9/SCL。
- 特征使用头部姿态减去躯干姿态。

~~~text
                         ┌─ 头部兼容 IMU，0x68
ESP32 GPIO8/9 I2C 总线 ─┤
                         └─ 躯干兼容 IMU，0x69
~~~

这是准确率升级，不阻塞第一天任务。

---

## 10. 固定测试矩阵

| 测试 | 次数/时长 | 黑客松通过标准 |
|---|---:|---|
| 静止坐姿 | 10 分钟 | ≤1 次误计数 |
| 标准 chin tuck | 20 次 | ≥17 次正确 |
| 低头 | 20 次 | ≤3 次误判 |
| 看手机 | 5 分钟 | ≤2 次误判 |
| 点头 | 20 次 | ≤3 次误判 |
| 左右转头 | 各 20 次 | ≤3 次误判 |
| 耸肩 | 20 次 | ≤3 次误判 |
| 走路 | 5 分钟 | ≤2 次误判 |
| 喝水 | 20 次 | ≤3 次误判 |
| 坐姿调整 | 20 次 | ≤3 次误判 |

每次固件或模型改变都完整重跑。所有失败样本必须保存。

---

## 11. 三天黑客松时间表

### 第 1 天：硬件和数据链路

- [x] 焊接 IMU 模块排针。
- [x] ESP32 串口通过（COM7 / CH343）。
- [x] I2C 稳定发现 0x68，且 `WHO_AM_I=0x70`。
- [x] 100.0 Hz 六轴 CSV 稳定，已验证 1175 行有效数据。
- [ ] Serial Plotter 显示动作曲线。
- [ ] 固定临时佩戴位置。

交付物：真实动作在电脑上形成稳定、可重复的曲线。

### 第 2 天：数据和识别

- [ ] 完成串口采集和标注工具。
- [ ] 采集至少 3 人数据。
- [ ] 对比 chin tuck、低头、点头、走路曲线。
- [ ] 完成基线规则和 Random Forest。
- [ ] 完成事件状态机和防重复计数。
- [ ] 生成精确率、召回率和混淆矩阵。

交付物：电脑实时判断动作并自动计数。

### 第 3 天：展示和可穿戴包装

- [ ] 固化实时识别流程。
- [ ] 完成 Streamlit 展示。
- [ ] 有余力再加 BLE，不让 BLE 阻塞主功能。
- [ ] 传感器固定到发箍、眼镜腿或挂耳支架。
- [ ] ESP32 放入口袋或肩部小袋。
- [ ] 完成十类干扰动作演示。
- [ ] 准备从当前原型到未来挂耳产品的演进图。

交付物：可佩戴、自动识别、自动计数、实时展示。

---

## 12. 团队分工

| 角色 | 任务 |
|---|---|
| 硬件/固件 | 焊接、接线、I2C、100 Hz 采样、USB/BLE |
| 算法/数据 | 采集、清洗、特征、分类、指标 |
| 前端/展示 | Streamlit/App、计数、曲线 |
| 产品/实验 | 规范动作、组织采集、记录误判、演示 |

如果只有两个人：

- A：硬件、Arduino 固件和 BLE。
- B：Python、模型和 Streamlit。
- 两人共同采集动作数据。

---

## 13. 决策门

严格按顺序执行：

~~~text
ESP32 串口通过
    ↓
I2C 发现 0x68
    ↓
六轴连续采样通过
    ↓
真实佩戴曲线可重复
    ↓
完整正负样本采集
    ↓
离线模型 ≥85%
    ↓
实时自动计数通过
    ↓
BLE
    ↓
最终外形和小型化
~~~

前一关未通过时不进入下一关。

必须暂停的情况：

- 板子或传感器发热。
- ESP32 随机重启。
- I2C 地址时有时无。
- 采样时间戳大量跳变。
- 佩戴后传感器会晃。
- 模型只对采集者本人有效。

---

## 14. 现在立刻做什么

1. 保持已经验证通过的五线连接；移动或调整导线前先拔掉 USB：

~~~text
ESP32 3V3   → 兼容 IMU VCC
ESP32 GND   → 兼容 IMU GND
ESP32 GPIO9 → 兼容 IMU SCL
ESP32 GPIO8 → 兼容 IMU SDA
ESP32 GND   → 兼容 IMU AD0
~~~

2. 保留当前兼容寄存器 Demo；不要为 `WHO_AM_I=0x70` 的现有模块强行切换到只按 MPU6050 识别的库。
3. 暂不连接压力传感器、光学模块和电池。
4. 把本轮 `COM7 / 0x68 / WHO_AM_I=0x70 / 100.0 Hz / 1175 行` 输出作为硬件基线记录保存。
5. 将 IMU 临时固定到眼镜腿、发箍或耳挂，保证它随头部运动且不会晃动。
6. 分别采集静止、chin tuck、低头、抬头、点头、转头、耸肩、走路、喝水、看手机和坐姿调整数据。
7. 先画出六轴和姿态曲线，确认动作信号可重复，再进入完整数据集采集。
8. 离线分类和自动计数达标后，再做 BLE 和最终外形。

---

## 15. 官方资料

- Espressif Arduino-ESP32 安装：
  https://docs.espressif.com/projects/arduino-esp32/en/latest/installing.html
- Espressif ESP32-S3 Arduino 支持：
  https://docs.espressif.com/projects/arduino-esp32/en/latest/getting_started.html
- Adafruit MPU6050 接线、库和示例（仅作接线参考；当前 0x70 模块使用兼容寄存器 Demo）：
  https://learn.adafruit.com/mpu6050-6-dof-accelerometer-and-gyro?view=all
- Stack Anyway：
  https://stack-anyway.com/
- Espressif 关于 Wokwi ESP32-S3 仿真：
  https://docs.espressif.com/projects/esp-idf/en/latest/esp32s3/third-party-tools/wokwi.html

---

## 16. 项目完成检查表

- [ ] 上电自动采集。
- [ ] 自动识别 chin tuck。
- [ ] 完整动作只计数一次。
- [ ] 干扰动作测试达到标准。
- [ ] USB 或 BLE 自动同步。
- [ ] 有多人数据和模型指标。
- [ ] 有失败案例和改进记录。
- [ ] 传感器佩戴位置固定。
- [ ] 连续演示 30 分钟无崩溃。

在这些条件通过前，耳夹、挂耳和披肩只是外形假设；通过后再根据传感器最佳位置、是否需要第二 IMU、重量和供电选择最终形态。
