# Pecky Cap（啄米帽）48 小时落地执行计划

> 版本：v2.0
>
> 日期：2026-08-27
>
> 执行周期：从团队确认本计划并启动计时起 H0–H48
>
> 最终交付：一顶可佩戴、带 3D 打印外壳、能本地识别并计数、通过 BLE 同步到 App 的 Pecky Cap 工程样机
>
> 产品边界：黑客松工程原型，不是量产设备、医疗器械或临床验证产品

---

## 0. 这份计划怎么用

这是一份 48 小时作战计划，不是长期技术百科。团队必须按时间顺序执行，每个阶段都包含：

1. **目标**：本阶段结束时必须得到什么。
2. **做法**：具体由谁、用什么、怎样完成。
3. **产出物**：能够保存、演示或交接的文件与实物。
4. **验收标准**：可以明确判断通过或失败的门槛。
5. **失败降级**：超时后砍什么，保证产品仍能交付。

### 0.1 优先级定义

| 优先级 | 含义 | 执行规则 |
|---|---|---|
| **P0** | 产品成立所必需 | 任一 P0 未通过，暂停 P1/P2；必须优先修复或执行降级 |
| **P1** | 让样机稳定、完整、像产品 | 只能在 P0 冻结后进行；失败时回滚 P0，不得拖累交付 |
| **P2** | 演示加分与外观润色 | 随时可以删除；最后 6 小时不得为了 P2 重构核心代码 |

### 0.2 48 小时硬规则

- P0 节点延误超过 2 小时：立即停止全部 P1/P2。
- H2 后冻结产品动作定义和 BLE 数据协议。
- H12 后禁止更换主控、主传感器、App 技术栈和核心交互。
- H24 必须形成可回滚的 `p0-demo` 版本和第一段真实演示视频。
- H42 后只修复阻断演示的问题。
- H46 后禁止新增功能，只允许构建、烧录、演练和回滚。
- 每个阶段完成后先测试，再提交并推送 GitHub。

---

## 1. 最终产品合同

### 1.1 一句话定义

**Pecky Cap 是一顶以 3D 打印刚性帽架为主体、帮助用户完成抗阻颈部训练的智能帽：用户无需打开 App，轻敲帽檐进入训练，完成一次符合要求且持续 5 秒的 chin tuck 后自动计数，完成 3 次后结束本组；记录通过 BLE 同步到“啄米”App，转换为米缸奖励和目标进度。**

### 1.2 固定用户流程

~~~text
戴上帽子
→ 轻敲帽檐，设备进入训练并给出提示
→ 双手置于后脑，完成抗阻 chin tuck
→ 后脑压力有效 + 头部姿态稳定 + 持续满 5 秒
→ 设备只计 1 次并反馈
→ 松开后重新武装
→ 完成 3 次，本组结束
→ ESP32 离线保存计数
→ App 打开或靠近后自动 BLE 同步
→ 小鸡啄米动画将奖励加入总米缸
→ 奶茶、潮玩、演唱会等目标同时更新百分比
~~~

### 1.3 什么算 1 次，什么绝对不能算

一次有效动作必须同时满足：

- 设备处于已开始的训练会话中。
- RFP-602 后脑压力超过个体校准阈值。
- 头部没有明显低头、抬头、转头或快速点头。
- 运动震动没有超过走路/晃动排除阈值。
- 有效状态连续保持 `5.0 s`；允许不超过 `150 ms` 的瞬时噪声。
- 上一次动作已经完全释放，压力回落至少 `500 ms`。
- 一次持续长按只计一次，不得每 5 秒重复计数。

以下情况不得计数：

- 低头、抬头、点头、左右转头。
- 耸肩、走路、喝水、看手机、正常坐姿调整。
- 只按压力片但不做规定姿态。
- 靠椅背或头枕造成后脑持续受压。
- 动作只保持 1–4.9 秒。
- 未进入训练会话时的日常动作。

### 1.4 计数与奖励解耦

设备只负责可靠地产生 `rep`、`set_complete` 和累计次数，不在固件里写死人民币规则。

App 负责奖励映射：

- 正常规则可设为“每完成 10 次训练 +¥5”。
- 黑客松演示模式可设为“完成 1 组（3 次）+¥5”，缩短现场演示时间。
- 总米缸只有一个余额；每个愿望目标用同一余额计算自己的进度。
- 目标进度：`min(100%, 总米缸金额 / 目标价格)`。

---

## 2. P0、P1、P2 交付边界

### 2.1 P0：产品必须成立

P0 最晚在 H24 完成，必须包含：

- 现有 ESP32-S3 + 现有 MPU6500-compatible IMU 稳定工作。
- RFP-602 压力传感器接入 ADC，和 IMU 形成联合判断。
- 本地完成轻敲开始、5 秒保持、释放再武装、3 次成组、防重复计数。
- 不依赖电脑完成识别和计数。
- BLE 广播、连接、事件 Notify、断线本地计数、重连快照同步。
- 一个能安装到真实手机的最小 App，至少显示连接、5 秒进度、次数、米缸余额。
- 一版真实可佩戴的 3D 打印帽架与电子舱；开发板、接头和裸焊点不得直接接触头部。
- 一段从真实动作到 App 米缸更新的无剪辑备份视频。

P0 明确不包含：定制 PCB、nRF52840/BMI270 迁移、云服务、账号体系、医疗效果宣称。

### 2.2 P1：把样机做完整

P1 在 H24–H42 完成：

- 3 人摘帽重戴测试和一次参数修正。
- 第二版/最终版 3D 打印外壳，解决装配、USB 开口、固定和走线。
- App 的米缸目标卡片、同步动画、离线去重和 BLE 自动重连。
- 30 分钟稳定性、5 次断电重启、5 次 BLE 断线重连测试。
- 线束应力释放、压力垫舒适层、可拆卸电子内衬。

### 2.3 P2：只做演示加分

P2 在 H42–H48 有余力才做：

- 更精细的小鸡啄米动画、蜂鸣器音效或 WS2812B 灯效。
- 外壳表面处理、Logo、配色、隐藏螺丝。
- 会话历史、数据导出、更多愿望目标编辑。
- 产品演进图：当前 ESP32 工程样机 → 未来 nRF52840/BMI270 定制 PCB。

以下内容无条件留到 48 小时之后：

- 自研 PCB、耳夹级小型化、OTA、量产结构。
- 原生 iOS 和 Android 双平台同时交付。
- TinyML/Random Forest 作为主识别路径。
- 会员、支付、真实资金转移和医疗结论。

---

## 3. 已完成基线：不要返工

| 项目 | 当前真实结果 | 状态 |
|---|---|---:|
| 主控 | ESP32-S3-N16R8 | 已确认 |
| 串口 | CH343，Windows `COM7` | 已确认 |
| IMU 地址 | I2C `0x68` | 已确认 |
| IMU 身份 | `WHO_AM_I=0x70`，按 MPU6500-compatible 处理 | 已确认 |
| 接线 | `3V3→VCC`、`GND→GND/AD0`、`GPIO8→SDA`、`GPIO9→SCL` | 已确认 |
| 采样 | 六轴数据 `100.0 Hz` | 已确认 |
| 硬件自检 | 15 秒收到 1175 条有效数据 | 已通过 |
| 采集工具 | 6 秒实测保存 599 条 CSV，`100.00 Hz` | 已通过 |
| 固件工具链 | Arduino ESP32 core 3.3.11，编译/烧录正常 | 已通过 |
| 数据环境 | Python、NumPy、Pandas、SciPy、sklearn、Streamlit、Bleak | 已安装 |
| 代码仓库 | `https://github.com/iboy425/pecky`，`main` 已关联 | 已完成 |

当前稳定文件：

- `firmware/01_hardware_demo/01_hardware_demo.ino`
- `tools/monitor_serial.py`
- `tools/capture_motion.py`
- `data/README.md`

禁止为了“代码更漂亮”重写已经通过的 I2C 和 100 Hz 采样层；后续在稳定代码上增量开发。

---

## 4. 最终软硬件架构

~~~mermaid
flowchart LR
    P[RFP-602 后脑压力片] -->|ADC| E[ESP32-S3]
    I[MPU6500-compatible IMU] -->|I2C 100Hz| E
    T[轻敲帽檐] -->|IMU 冲击特征| E
    E --> F[校准与滤波]
    F --> S[本地状态机]
    S --> C[本地计数 + NVS 保存]
    S --> H[板载 LED/蜂鸣器反馈]
    C -->|BLE Notify + Snapshot| A[手机 App]
    A --> R[奖励规则]
    R --> J[总米缸]
    J --> G1[奶茶目标百分比]
    J --> G2[潮玩目标百分比]
    J --> G3[演唱会目标百分比]
~~~

核心原则：

- 原始传感器在本地采集。
- 动作识别、5 秒计时和计数在 ESP32 完成。
- 手机断开时设备仍工作。
- BLE 只传状态、事件和快照，不以 100 Hz 持续传全部原始六轴数据。
- App 负责展示、奖励、动画和目标管理，不决定一次动作是否有效。

---

## 5. 硬件实施方案

### 5.0 可用硬件白名单

本方案以 `She Nicest黑客松硬件物料清单.xlsx` 为唯一硬件白名单，并结合已经拍照、接线和实测的器件。48 小时内不要求购买或定制清单外电子模块。

本产品实际允许使用：

- `ESP32 S3核心板板载1-N16R8`。
- `(BCG)高性能6轴MEMS运动跟踪传感器模块`；当前实物 `WHO_AM_I=0x70`。
- `RFP602压力传感器`。
- `电阻包`。
- `面包板` 和 `杜邦线（公公、公母、母母）`。
- 模组套盒中的有源蜂鸣器、按键、敲击传感器、3 色 LED 等；P0 优先用开发板板载 RGB LED，减少接线。
- `WS2812B灯带`；只作为 P1/P2 灯效，不阻塞识别。
- `UNO R3移动电源电池盒+电池`；只按其稳定输出给 ESP32 供电，不自制充电电路。
- Snapmaker U1、Artisan 或现场拓竹 3D 打印机及打印机当前提供的耗材。
- 清单中的电烙铁、胶枪、电磨笔、螺丝刀和工具箱。

最终产品载体优先直接使用现场 3D 打印机制造，不要求另购布帽。现有布帽只作为打印失败时的降级载体，不能用渲染图代替真实打印与装配。

### 5.1 48 小时内使用的硬件

| 部件 | 用途 | 优先级 | 处理方式 |
|---|---|---:|---|
| ESP32-S3-N16R8 开发板 | 本地识别、存储、BLE | P0 | 沿用现有板，不换 nRF52840 |
| 现有蓝色 IMU | 姿态、角速度、轻敲和走路排除 | P0 | 按 MPU6500-compatible 驱动 |
| RFP-602 薄膜压力片 | 识别后脑抗阻压力 | P0 | 放在帽子原有内衬外侧与 3D 打印分力片之间 |
| 电阻包中的 10 kΩ 电阻 | 与压力片构成分压 | P0 | 若包内无 10 kΩ，实测选择包内 4.7–47 kΩ 的一个固定值 |
| 3D 打印帽架与外壳 | 可穿戴主体、主控舱、IMU 座、压力座、走线 | P0 | 使用现场打印机现有耗材，模块化打印 |
| 团队现有帽子 | 打印失败时的降级载体 | P0 降级 | 不是主路径，不新增电子硬件 |
| UNO R3 移动电源电池盒+电池 | 脱离电脑供电 | P0 | 先确认输出，再经开发板 USB/确认的 5 V 入口供电 |
| ESP32 板载 RGB LED | 开始、成功、完成和错误反馈 | P0 | 外壳预留导光孔，不增加模块 |
| 模组套盒有源蜂鸣器 | 可选声音反馈 | P1 | 仅在板载 LED 和核心闭环稳定后接入 |
| WS2812B 灯带 | 可选展示灯效 | P2 | 不进入 P0 关键链路 |

不要在 48 小时内：

- 画并投产定制 PCB。
- 临时制作清单之外的锂电充电电路。
- 把电池盒输出接到未经确认的开发板引脚。
- 为了增加振动而临时购买马达、MOSFET 或驱动板。
- 为了和概念图一致而更换 nRF52840、BMI270。

### 5.2 固定接线

IMU 保持已验证接线：

| ESP32-S3 | IMU | 作用 |
|---|---|---|
| 3V3 | VCC | 3.3 V 供电 |
| GND | GND | 共地 |
| GPIO8 | SDA | I2C 数据 |
| GPIO9 | SCL | I2C 时钟 |
| GND | AD0 | 地址固定为 `0x68` |

RFP-602 推荐分压：

~~~text
3V3 ── RFP-602 ──┬── GPIO4（ADC1）
                  │
          电阻包中的 10 kΩ
                  │
GND ──────────────┘
~~~

接线后必须实测：

- 无压力时 ADC 基线。
- 正常训练压力时 ADC 值。
- 用力按压时是否饱和。
- 连续 30 秒是否漂移。

如果 GPIO4 与实际开发板用途冲突，改用另一个已确认的 ADC1 引脚，并在固件和接线图中只保留一个最终定义。

### 5.3 P0 识别状态机

~~~text
SLEEP/IDLE
  → 检测到帽檐轻敲
ARMING
  → 3 秒中立位与压力基线校准
READY
  → 压力超过 P_on 且姿态有效
CANDIDATE
  → 连续稳定计时 0–5 秒
  → 压力掉落/姿态无效：回 READY，不计数
SUCCESS
  → count +1，只触发一次反馈与 BLE 事件
WAIT_RELEASE
  → 压力低于 P_off 连续 500 ms
READY
  → 第 3 次完成后 SET_COMPLETE，再回 IDLE
~~~

阈值做法：

- 上电/开始训练后采集 3 秒中立位基线。
- 压力开启阈值 `P_on` 高于基线和噪声带。
- 释放阈值 `P_off < P_on`，形成迟滞，防止边缘抖动。
- 姿态门控主要使用 pitch/roll、三轴角速度和加速度模长变化。
- 第一版使用可解释规则，不把训练机器学习模型作为 P0 前置条件。

### 5.4 你的硬件主线：先把动作捕捉做准

你负责的工作严格按下面顺序进行；上一关没有数据证明通过，不进入下一关。

#### Gate H1：裸模块数据质量

**做法**

1. 保持现有 IMU 五线接法，固定 `100 Hz`。
2. 模块静止 60 秒，记录时间戳、六轴、温度和加速度模长。
3. 分别绕三个轴缓慢转动，确认轴向和正负号。
4. 压力片接入后，记录无压力、正常训练压力、最大安全按压各 20 次。

**通过标准**

- 时间戳严格递增。
- 采样间隔中位数约 `10 ms`，P95 在 `8–12 ms`。
- 60 秒丢样率低于 `0.1%`，没有连续 `>50 ms` 空洞。
- 静止时加速度模长均值在 `0.95–1.05 g`。
- 压力基线与训练压力范围不重叠，ADC 不长期饱和。

#### Gate H2：正确动作能被稳定捕捉

**做法**

1. 先不做 App、不做动画；把 IMU 临时刚性固定在 3D 打印头环试片上。
2. 将 RFP-602 固定在后脑压力座的预定位置。
3. 同一人以同一节奏完成 20 次标准动作，每次前后保留 2 秒中立位并保持 5 秒。
4. 画出 pressure、pitch、roll、gx/gy/gz、acc_mag 和状态曲线。
5. 对每次动作计算压力峰值、5 秒内波动、姿态偏移和角速度峰值。

**通过标准**

- 20 次中至少 18 次存在清晰的压力起点、稳定保持段和释放点。
- 5 秒保持段压力不得频繁跨越阈值；单次中断总时长不超过 `150 ms`。
- 同一佩戴下关键特征的变异系数建议低于 `20%`；超出时优先修固定结构，不先调算法。
- 人工视频标记与传感器起止时间误差不超过 `200 ms`。

#### Gate H3：重新佩戴仍可复现

**做法**

1. 摘下并重新戴上 3D 打印帽架 5 次。
2. 每次完成 3 个标准动作。
3. 每次记录校准零位、压力基线和动作特征。

**通过标准**

- 5 次重戴均能完成校准。
- IMU 方向没有翻转或明显旋转。
- 压力基线漂移不超过该测试者有效压力增量的 `20%`；超过时修改压力座深度/位置。
- 15 次动作至少 13 次被同一套规则识别。

#### Gate H4：再加入干扰动作

正确动作通过 Gate H1–H3 后，才加入低头、抬头、点头、转头、耸肩、走路、喝水、看手机、坐姿调整、靠椅背和只按压力片。失败时必须保存原始 CSV 和动作视频时间点，不能只改阈值后删除失败记录。

### 5.5 固件输出分层

- **研发 USB 模式**：以 100 Hz 输出原始六轴、压力、姿态和状态，用于保存 CSV；这是数据准确性的证据链。
- **产品 BLE 模式**：本地仍以 100 Hz 识别，只以 `5–10 Hz` 发送保持进度，并在事件发生时立即发送 rep/snapshot；不通过 BLE 连续发送所有原始数据。
- 两种模式共用同一采样、滤波和状态机，禁止维护两套不同识别逻辑。

---

## 6. BLE 数据协议

### 6.1 固定原则

- H2 前一次性冻结 UUID 和消息字段。
- 设备名：`Pecky-XXXX`，后四位来自芯片 ID。
- 使用一项自定义 Service，三项 Characteristic：
  - `EVENT`：Notify，发送 rep、进度和完成事件。
  - `SNAPSHOT`：Read/Notify，发送累计计数和最后序号。
  - `COMMAND`：Write，接收 sync、calibrate、reset_session。
- 每个事件带递增 `seq`；App 按 `seq` 去重。
- ESP32 使用 NVS 保存 `seq`、累计 rep 和未同步事件摘要。

具体 UUID 在 H0–H2 写入 `docs/ble-protocol.md`，写入后不得临时改变。

### 6.2 消息格式

事件示例：

~~~json
{"v":1,"type":"rep","seq":27,"session":4,"rep":2,"set_target":3,"hold_ms":5010,"device_ms":183020}
~~~

组完成示例：

~~~json
{"v":1,"type":"set_complete","seq":28,"session":4,"reps":3,"device_ms":189600}
~~~

连接后快照：

~~~json
{"v":1,"type":"snapshot","last_seq":28,"total_reps":43,"pending_sets":1,"battery":null}
~~~

App 命令：

~~~json
{"v":1,"cmd":"sync","after_seq":24}
~~~

### 6.3 BLE 验收

- 实体手机 10 秒内发现并连接设备。
- 连续 20 个事件，App 收到 20 个且无重复。
- 主动断开 5 次，均能重新连接。
- 断线期间完成动作，设备继续计数。
- 重连后 Snapshot 恢复最终累计值。
- App 杀进程重开后，不重复发放已经入米缸的奖励。

### 6.4 准确与低延迟指标

- ESP32 采样/识别循环：`100 Hz`，不得因 BLE 发送阻塞。
- 保持进度 Notify：`5–10 Hz` 即可；队列拥堵时优先保留 rep 和 snapshot。
- rep 事件在本地判定成功后立即 Notify，不等待下一秒批处理。
- 每条消息携带 `seq` 和 `device_ms`；App 记录 `received_at` 和处理结果。
- BLE 事件到 App 状态更新：中位数目标 `<150 ms`，P95 `<300 ms`。
- 端到端延迟用同一段高帧率视频测量：ESP32 板载 LED 在发出 rep 时同步点亮，逐帧比较 LED 与 App 计数变化，至少测 20 次。
- 20 次测试必须满足：事件 `seq` 连续、App 显示值一致、没有重复奖励；若漏事件，Snapshot 必须在重连/对账时修正最终累计值。
- 原始动作准确性以 USB CSV 为准，BLE 只负责传事件，不能用手机曲线代替传感器原始记录。

---

## 7. App 落地方案

### 7.1 技术栈必须在 H1 前二选一

| 现有条件 | P0 选择 | 说明 |
|---|---|---|
| Windows + Android 实体机 | Flutter + `flutter_blue_plus` | 默认路径，能在 Windows 构建并装到 Android 实机 |
| Mac + iPhone + Xcode | SwiftUI + CoreBluetooth | 如果现场明确以 iPhone 演示，使用这条路径 |
| 只有 Windows + iPhone | 不可承诺真实 iOS App | H1 前借到 Android 或分配一台 Mac；网页蓝牙不能伪装成 iOS 原生 BLE |

禁止同时开发 Flutter、SwiftUI 和网页三个版本。产品负责人必须在 H1 前写下唯一演示手机型号。

### 7.2 P0 页面

1. **设备/训练页**
   - 连接状态、设备名、最后同步时间。
   - 当前状态：未开始、校准、准备、保持中、成功。
   - 5 秒环形进度。
   - 本组 `0/3`、`1/3`、`2/3`、`3/3`。

2. **米缸首页**
   - 总米缸金额。
   - 奶茶、潮玩、演唱会等目标卡片。
   - 每张卡片显示价格、还差多少和百分比。
   - 收到新组时显示简单“+¥5 入仓”反馈。

### 7.3 P1 页面和动画

- App 打开后如果有离线同步事件，小鸡将对应数量的米啄入米缸。
- 多个历史事件批量同步时播放一次汇总动画，避免几十次重复动画。
- 目标示例：
  - 奶茶 ¥50：余额达到 ¥50 时显示“现在可以买”。
  - 潮玩 ¥70：余额 ¥50 时显示约 71%。
  - 演唱会 ¥900：余额 ¥50 时显示约 6%。
- App 本地持久化：总米缸、目标列表、最后处理 `seq`、奖励规则。

### 7.4 App 验收

- 不打开 App 也能完成 3 次训练并在设备端保存。
- 打开 App 后自动连接或只需一次选择设备。
- 新事件到达后 1 秒内更新次数和进度。
- 3 次完成后奖励只入账一次。
- 总米缸变化后所有目标百分比同时正确更新。
- 杀进程重开、BLE 重连均不重复加钱。

---

## 8. 3D 打印帽架与电子舱

### 8.1 结构目标

主路径不是打印一个厚重、完全封闭的头盔，而是打印一套轻量、开放、模块化的“帽子骨架”：

- 可调节头环决定佩戴位置和重复性。
- 帽檐提供外观和轻敲入口。
- 前上方刚性 IMU 座固定传感器方向。
- 左右电子舱分别放主控和 UNO R3 移动电源电池盒，尽量平衡重量。
- 后脑压力座将 RFP-602 固定在枕骨中央。
- 顶部使用 2–4 条轻量弧形骨架连接，不打印大面积实心穹顶。
- 所有导线进入打印走线槽；主板、接头、焊点不接触皮肤和头发。

这种结构比软帽更有利于数据准确：IMU 的前后/上下方向固定，压力片位置可重复，摘戴后误差更小。

### 8.2 打印分件

1. **可调头环**
   - 测量主要演示者的头围、左右宽和前后长。
   - 头环分成前、后或四段，确保放进打印平台。
   - 使用打印卡槽进行 2–3 档调节；卡槽失败时使用清单中胶枪固定到已验收尺寸。

2. **帽檐**
   - 与前头环连接，能承受轻敲而不整体晃动。
   - 不要求承载电子主板；避免前部过重。

3. **IMU 刚性座**
   - 位于前额上方或帽架顶部前侧，和头环刚性连接。
   - 在模型和实物上标记 `FRONT`、`UP`、X/Y/Z。
   - 模块插入后不能旋转、滑动或悬空；安装方向每次一致。

4. **ESP32 电子舱**
   - 安装在帽架一侧，包含底座、卡扣上盖、USB、RST/BOOT、板载 RGB LED 导光孔和线束出口。
   - 内腔按实测主板每侧增加 `0.5–0.8 mm` 余量。

5. **电池盒固定舱**
   - 安装在另一侧或后侧，只适配清单中的 UNO R3 移动电源电池盒。
   - 必须先确认输出与开发板供电方式，再确定开口和线长。

6. **后脑压力座**
   - 居中接触枕骨区域，采用圆角分力片覆盖 RFP-602 有效受力区。
   - 设计滑槽或 2–3 档深度，使不同头型都能接触但不会持续夹痛。
   - RFP-602 尾线进入封闭线槽并有应力释放。

7. **走线夹/槽**
   - 沿头环布置 3D 打印卡线槽，不依赖额外线夹和紧固件。
   - 所有连接转角保留弯曲半径，不能折压压力片尾线。

### 8.3 打印顺序

1. H0–H2：测量头部和全部真实硬件。
2. H2–H4：打印 `20–40 mm` 的头环弧度试片、主板宽度试片、USB 开口试片和压力座小样。
3. H4–H6：试戴并修正头环曲率、压力深度和卡扣间隙。
4. H6–H10：低填充打印 v0 模块化帽架；打印期间并行开发固件和 App。
5. H18–H22：把硬件装入 v0，验证动作信号和佩戴稳定性。
6. H28–H33：只修正 v0 暴露的问题，打印最终 v1。

不要在未验证头环弧度和板卡间隙前直接打印整顶帽架。

### 8.4 CAD 与打印参数

- 软件只选团队最熟悉的一种：Fusion 360、Onshape 或 FreeCAD。
- 使用现场 Snapmaker/拓竹打印机当前提供的材料，不把新材料列为前置条件。
- 壁厚以 `1.6–2.0 mm` 起步；头环和卡扣受力区局部加厚。
- 所有皮肤侧边缘圆角不小于 `2 mm`。
- 大部件优先开放骨架、低填充；强度不足只在受力处加筋。
- 设计应模块化；单个部件打印失败时不需要重打整顶帽子。
- 保存每一版 `STEP`、`STL`、切片参数、打印方向和实际打印时长。

### 8.5 帽架验收

- 主板、IMU、RFP-602 和电池盒全部能进入各自固定位置。
- USB 插头不拆壳即可插拔，RST/BOOT 可访问。
- IMU 连续摘戴 5 次后方向和零位变化满足第 10 节数据门槛。
- 摇头、低头和走路时板卡、IMU、压力座不松动、不异响。
- 连续佩戴 10 分钟无明显夹痛、尖角、焊点或接头接触。
- 走线不拉扯压力片、不卡头发。
- 运行 30 分钟没有异常升温、软化和变形。
- 戴帽和摘帽各 10 次后整机仍正常工作。
- v0 或 v1 必须是实物打印件，不能用 CAD 渲染截图代替。

---

## 9. 48 小时时间线

## P0：H0–H24，先让产品成立

### P0-0｜H0–H2：冻结产品、平台和协议

**目标**

- 全队对“什么算一次、演示哪台手机、最终实物长什么样”只有一个答案。

**做法**

1. 指定一顶帽子、一块 ESP32、一块 IMU、一片 RFP-602 为黄金硬件。
2. 冻结 5 秒、3 次、轻敲开始和释放再武装规则。
3. 在 Android/Flutter 与 iPhone/SwiftUI 中只选一条 App 路径。
4. 冻结 BLE UUID、消息字段和 `seq` 去重方案。
5. 用卡尺测量主板、IMU、USB 插头、帽檐厚度和可安装空间。
6. 四条工作线同时启动：固件、App、结构、测试/产品。

**产出物**

- `docs/product-contract.md`
- `docs/ble-protocol.md`
- 带尺寸照片和 CAD 草图
- 唯一演示手机型号

**验收标准**

- 团队成员能在 30 秒内说清同一套用户流程。
- 所有待开发模块拥有负责人和 H24 截止点。
- 没有“后面再决定”的主控、传感器、App 平台和通信协议。

**失败降级**

- H2 仍有争议时，由产品负责人按本计划默认值直接冻结，不继续讨论。

### P0-1｜H2–H6：正确动作信号、App 骨架和结构试片

**目标**

- 先证明标准颈部动作能被硬件稳定捕捉；同时让 App 能构建安装，并验证头环/板卡关键尺寸。

**做法**

1. 按分压电路将 RFP-602 接入 ADC1。
2. 固件同时输出六轴、压力原值、压力校准值和状态。
3. 先完成 Gate H1，再将 IMU 和压力片临时固定到头环试片，采集 20 次标准 5 秒动作并完成 Gate H2。
4. 在曲线上确认信号重复性后，冻结 IMU 方向和后脑压力位置；信号未通过前不打印整帽架。
5. App 队友建立两个页面和 BLE 数据模型，使用固定的模拟事件先跑 UI，不等待硬件。
6. 结构线只打印头环弧度、主板宽度、USB、卡扣和压力座小试片。

**产出物**

- `firmware/02_pressure_imu_stream/`
- 20 次标准动作的 USB CSV、曲线和人工视频时间点
- 能装到实体手机的 App 空壳
- 头环/主板/压力座试片和 `cad/v0-fit-test.*`

**验收标准**

- Gate H1、Gate H2 全部通过；20 次正确动作至少 18 次具有清晰起点、保持和释放。
- ADC 不长期为 0 或满量程，连续 5 分钟无断线；USB 数据满足时间戳和丢样率门槛。
- App 在演示手机上启动，无崩溃。
- 传感器位置和方向已冻结；测试片误差已记录并反映到帽架 v0。

**失败降级**

- RFP-602 信号方向或幅度异常：改用电阻包内另一阻值并重测，不换主控。
- 20 次动作重复性差：先修改 IMU 刚性座和压力座位置，不先做 BLE 或调复杂算法。
- CAD 超时：使用简单开放头环 + 两件式矩形电子舱，不做复杂曲面和隐藏卡扣。

### P0-2｜H6–H10：本地状态机、BLE 最小闭环、外壳首打

**目标**

- 设备能独立完成一次 5 秒计数；手机能收到真实事件；完整粗外壳进入打印机。

**做法**

1. 在已验证 100 Hz 采样代码上加入压力滤波和状态机。
2. 实现 3 秒校准、`P_on/P_off` 迟滞、5 秒保持和释放再武装。
3. 先用串口逐状态输出，完成后再加入 BLE。
4. ESP32 建立 Service 和 EVENT/SNAPSHOT/COMMAND。
5. App 扫描 `Pecky-XXXX`，显示连接状态和原始事件。
6. 低填充打印 v0 完整电子舱；打印期间固件和 App 并行开发。

**产出物**

- `firmware/03_recognition_ble/`
- App 首次真实 BLE 连接录像
- `cad/v0-enclosure.*` 和打印件 v0

**验收标准**

- 10 次满 5 秒动作均只产生一个 rep。
- 10 次 1–4 秒短按均不计数。
- 一次持续 12 秒压力只计一次。
- 手机连续收到 10 个递增 `seq`，无重复、无漏失。
- v0 能放入主板且 USB 可插入。

**失败降级**

- 轻敲识别不稳定：P0 临时使用板载按键开始会话，但保留“不打开 App”的原则。
- BLE Notify 失败：先只发最小短 JSON，不传进度和原始传感器。

### P0-3｜H10–H14：干扰动作排除和 App 实时训练页

**目标**

- 系统开始真正区分 chin tuck 与日常动作，App 实时显示 5 秒进度和 `0/3`。

**做法**

1. 使用 `tools/capture_motion.py` 采集：chin tuck、低头、抬头、点头、转头、耸肩、走路、喝水、看手机、坐姿调整、靠椅背、只按压力片、短时保持。
2. 先由 1 名开发者每类做 5 次，画出压力、pitch/roll、角速度和加速度模长。
3. 用阈值和状态机排除明显错误，不训练复杂模型。
4. App 订阅进度事件，显示保持环和本组次数。
5. ESP32 每次计数后立即写 NVS 摘要，不能只存在 RAM。

**产出物**

- 开发数据和阈值记录
- `docs/recognition-rules.md`
- 实时训练页

**验收标准**

- 15 次正确动作至少 13 次成功，且没有重复计数。
- 60 次指定干扰动作中不出现“完成一组”；错误 rep 不超过 2 次并有日志。
- 10 次靠椅背和 10 次只按压力片不能完成计数。
- App 事件更新延迟不超过 1 秒。

**失败降级**

- 单 IMU 无法排除某项时，提高压力/姿态门槛并明确演示姿势范围。
- 若仍无法区分，保留轻敲启动 + 压力 + 5 秒状态机，公开记录该干扰为已知限制，不临时加入第二 IMU。

### P0-4｜H14–H18：米缸闭环和离线同步

**目标**

- 完成 3 次后，App 只奖励一次；设备离线完成后重连也能正确入米缸。

**做法**

1. 设备生成 `rep` 和 `set_complete` 两种事件。
2. App 使用 `seq` 去重，保存 `last_processed_seq`。
3. 实现总米缸和三个固定目标卡片。
4. 演示模式设置为“一组 +¥5”，正常规则保留配置入口但不在现场修改。
5. 测试 App 关闭、BLE 断开、设备重启后的同步。

**产出物**

- 可安装 App 包
- BLE 离线/重连测试记录
- 米缸首页

**验收标准**

- 3 次动作只产生一次奖励。
- 断开 App 后完成 1 组，重连后只补发一次奖励。
- App 杀进程重开 5 次，余额不重复增长。
- 所有目标百分比与同一总米缸金额计算一致。

**失败降级**

- 无法保存完整事件队列时，设备至少保存累计 rep、累计 set 和最后 `seq`，App 用 Snapshot 对账。
- 动画不稳定时只显示“+¥5 入仓”数字反馈，动画移到 P1。

### P0-5｜H18–H22：装入 3D 打印帽架并形成实体产品

**目标**

- 电子模块离开面包板形态，装进 3D 打印帽架和电子舱。

**做法**

1. 检查 v0 外壳尺寸，必要时只改阻断装配的问题并快速复打。
2. 使用短线、焊接或可靠连接器替换容易脱落的长杜邦线。
3. IMU 刚性固定并标记方向。
4. 后脑压力片装入圆角 3D 打印分力片和固定座，并利用帽子原有内衬隔离皮肤。
5. 主控放入外壳，上盖关闭，USB 和按键可访问。
6. 线束沿打印走线槽固定；清单内电池盒固定在帽架侧面/后侧，与主控尽量平衡重量。

**产出物**

- 可佩戴 Pecky Cap v0 实物
- 实物装配照片、重量和走线图
- 装配顺序文档

**验收标准**

- 戴帽 10 分钟不移位、不夹痛、不露焊点。
- 戴/摘 10 次后功能正常。
- 摇头、走路时模块不松动，线束不拉扯传感器。
- 实物完成 3 次训练并成功同步 App。

**失败降级**

- 完整上盖来不及复打：保留已通过的底座，使用透明绝缘上盖/固定带封闭；不得裸板贴头。
- 电池盒输出或接口未确认：P0 验收先用 USB 供电；继续检查清单中的 UNO R3 移动电源电池盒，不接入清单外电池。

### P0-6｜H22–H24：P0 验收、冻结和备份

**目标**

- 获得第一个可随时回滚、可完整演示的真实产品版本。

**做法**

1. 从上电开始完整演示：轻敲、校准、短动作失败、3 次成功、BLE 同步、米缸更新。
2. 连续运行 15 分钟。
3. 关闭 App 完成一组，再重连验证补同步。
4. 录制一段无剪辑备份视频。
5. 保存固件二进制、App 安装包、STL/STEP、测试结果。
6. Git 提交、推送并打 `p0-demo` 标签。

**验收标准**

- 产品闭环连续成功 3 次。
- 无重复计数、重复奖励和崩溃。
- 3D 外壳已实际安装，不以渲染图代替。
- 另一名队员按文档能重新上电并完成一次演示。

**失败降级**

- 任一 P0 未通过：停止全部 P1/P2，回到最近通过节点；H24 后不再扩展功能。

## P1：H24–H42，把样机做稳定

### P1-0｜H24–H28：摘帽重戴盲测与唯一一次算法修正

**目标**

- 验证产品不是只对开发者和一次佩戴位置有效。

**做法**

1. 3 名测试者各摘帽重戴后完成 3 组，每组 3 次。
2. 每人执行主要干扰动作。
3. 固件开发者不提示动作结果，测试者完成后统一看日志。
4. 只允许一次阈值/状态机集中修正。

**产出物**

- 盲测表、失败样本、修正说明

**验收标准**

- 27 次目标动作至少 24 次正确，无一次动作重复计数。
- 主要干扰动作共 90 次，错误 rep 不超过 2 次且不能完成错误组。
- 修正后重新跑 1 套未参与调参的数据。

**失败降级**

- 明确限定为“佩戴后校准的单用户演示样机”，不宣称多人泛化。

### P1-1｜H28–H33：最终外壳二次打印与 App 动画

**目标**

- 得到可展示的最终外壳和品牌化米缸反馈。

**做法**

1. 根据 v0 实装问题只修改尺寸、固定、USB、走线和舒适性。
2. 打印最终外壳；打印期间 App 开发同步动画。
3. 小鸡动画只消费已经去重的 `set_complete` 事件。
4. 保存最终 STEP/STL 和切片参数。

**产出物**

- 最终打印件 v1
- 米缸同步动画
- `cad/release/` 文件

**验收标准**

- 打印件不靠手按就能固定，拆装 3 次无损坏。
- 动画播放失败不影响余额正确性。
- 离线同步多组时使用汇总动画且金额正确。

**失败降级**

- 最终打印失败立即回滚可用 v0；不为了外观牺牲已通过的固定和安全。

### P1-2｜H33–H37：最终装配、供电与容错

**目标**

- 完成现场版整机，并能从常见错误中恢复。

**做法**

1. 最终装配和线束应力释放。
2. 加入 I2C 断线、ADC 饱和、采样超时和 BLE 断连状态。
3. 异常时停止计数并显示错误，不输出假成功。
4. 测试 5 次断电重启和 5 次 BLE 重连。

**验收标准**

- 5 次冷启动均能校准、连接和完成一组。
- 断开 IMU/压力片时进入错误状态且不计数。
- BLE 断开不影响本地计数，重连最终一致。

**失败降级**

- 蜂鸣器、WS2812B 等非核心部件故障直接拆除，用板载 LED + App 反馈。

### P1-3｜H37–H40：完整回归和 30 分钟稳定性

**目标**

- 用固定测试矩阵证明现场版足够稳定。

**做法**

1. 运行第 10 节完整验收矩阵。
2. 连续运行 30 分钟，记录采样率、崩溃、温度和 BLE 状态。
3. 检查米缸金额、目标百分比和事件序号。

**验收标准**

- 采样率保持 `95–105 Hz`。
- 30 分钟无崩溃、无异常重启、无持续采样中断。
- BLE 事件无重复奖励。
- 外壳、线束、压力垫没有移位。

**失败降级**

- 回滚 `p0-demo`；只修复能稳定复现且阻断演示的问题。

### P1-4｜H40–H42：版本冻结

**目标**

- 形成最终候选版本，不再重构。

**做法**

1. 冻结固件、App、CAD 和演示数据。
2. 从干净仓库重新构建并烧录一次。
3. 生成 `release-candidate` 标签和发布清单。

**验收标准**

- 新电脑/干净目录按 README 能完成构建。
- 固件二进制、App 安装包和 STL 均有校验和或明确版本号。
- 所有已知限制写入文档。

**失败降级**

- 直接使用 `p0-demo` 及其配套 App/外壳，不混用不同版本。

## P2：H42–H48，只做演示加分与发布

### P2-0｜H42–H45：外观、动效和说明材料

**目标**

- 让评委一眼看懂产品价值和内部结构。

**做法**

1. 加 Logo、贴纸、外壳表面处理和必要导光。
2. 优化小鸡动画、目标卡片和演示数据。
3. 制作一张“现有工程样机 → 未来定制 PCB”的演进图。

**验收标准**

- 任一改动后连续完成 3 个真实训练组。
- 不改变 BLE 协议、识别状态机和存储格式。

**失败降级**

- 整体删除 P2 改动，回滚 `release-candidate`。

### P2-1｜H45–H47：演示彩排和备份

**目标**

- 任何队员都能在 3 分钟内稳定完成演示。

**做法**

1. 按第 12 节脚本至少彩排 3 次。
2. 准备清单中的胶枪、杜邦线、电阻包、USB 线、UNO R3 移动电源电池盒和已烧录开发板。
3. 将备份视频、App 包、固件、STL 和演示 PPT 放到两处存储。

**验收标准**

- 非开发者独立完成一次全流程。
- 断网状态下仍能完成本地识别、BLE 和 App 演示。

**失败降级**

- 使用现场稳定版实物 + 无剪辑备份视频，不现场改代码。

### P2-2｜H47–H48：发布

**目标**

- 锁定提交，准时交付。

**做法**

1. 最后一次设备电量、连接、外壳和 App 检查。
2. 打 `hackathon-release` 标签并推送 GitHub。
3. 禁止新增功能。

**验收标准**

- 实物、手机、安装包、源码、外壳文件、测试结果和备份视频全部齐全。
- 最终提交与现场运行版本完全一致。

---

## 10. 固定验收矩阵

| 类别 | 测试 | P0 门槛 | P1 门槛 |
|---|---|---:|---:|
| 采样 | 连续采样率 | 95–105 Hz，15 分钟 | 95–105 Hz，30 分钟 |
| 采样 | 时间戳/丢样 | 单调，P95 间隔 8–12 ms，丢样 <0.1% | 同 P0，且无 >50 ms 连续空洞 |
| 信号 | 正确动作重复性 | 20 次中 ≥18 次有清晰起点/保持/释放 | 重戴 5 次后仍满足规则 |
| 正确动作 | 标准 5 秒 chin tuck | 15 次中 ≥13 | 3 人共 27 次中 ≥24 |
| 防重复 | 一次持续 12 秒 | 5 次均只计一次 | 10 次均只计一次 |
| 时长门控 | 保持 1–4 秒 | 10 次均不计 | 20 次均不计 |
| 日常干扰 | 低头/抬头/点头/转头/耸肩/走路/喝水/看手机/坐姿调整 | 每类 5 次；不得完成错误组 | 合计 90 次；错误 rep ≤2，不得完成错误组 |
| 压力对抗 | 只按压力片 | 10 次不计 | 15 次不计 |
| 场景对抗 | 靠椅背/头枕 | 10 次不计 | 15 次不计 |
| BLE | 连续事件 | 20 条无漏失/重复 | 50 条无漏失/重复 |
| BLE | 断开重连 | 3 次后计数一致 | 5 次后计数一致 |
| BLE | 事件到 App 延迟 | 中位数 <150 ms，P95 <300 ms | 20 次测量均无错误计数 |
| App | 重启去重 | 3 次不重复奖励 | 5 次不重复奖励 |
| 外壳 | 佩戴 | 10 分钟不移位 | 30 分钟不移位 |
| 整机 | 冷启动 | 3/3 成功 | 5/5 成功 |

所有指标必须表述为“小样本工程验收”，不得包装成临床准确率或普适性结论。

---

## 11. 团队并行分工

| 工作线 | H0–H24 P0 负责人任务 | H24–H42 P1 负责人任务 |
|---|---|---|
| 硬件/固件 | 压力 ADC、状态机、NVS、BLE、反馈 | 容错、参数修正、稳定性 |
| App | BLE 实机、训练页、米缸、去重 | 动画、自动重连、安装包 |
| 结构/3D | 头部/硬件测量、帽架 CAD v0、试片、首打 | 最终帽架/外壳、装配和外观 |
| 测试/产品 | 动作规范、数据采集、阶段验收 | 盲测、回归、演示材料 |

当前两人分工按下面执行：

- **你（硬件主责）**：现有接线保护、RFP-602 分压、100 Hz 数据质量、正确动作 20 次采集、传感器位置、识别状态机、ESP32 BLE、3D 帽架/电子舱装配和最终烧录。
- **队友（软件主责）**：App 技术栈、模拟事件 UI、BLE 扫描/连接/去重、训练页、米缸、目标百分比、安装包和软件日志。
- **共同完成**：产品协议冻结、动作规范、重戴测试、干扰动作、延迟录像、最终彩排。
- 打印期间不能等待：你继续做 USB 数据/固件，队友继续用模拟数据做 App。
- 你交给队友的不是口头描述，而是 `docs/ble-protocol.md`、示例 JSON 和一个持续发送模拟事件的固件版本。

每日状态只用四列：`任务 / 负责人 / 截止小时 / 验收是否通过`。不要用“快好了”“基本能跑”作为状态。

---

## 12. 3 分钟最终演示脚本

1. **20 秒：问题与产品**

   展示 Pecky Cap 实物和 3D 外壳，说明用户不需要打开 App 开始训练。

2. **20 秒：开始与校准**

   轻敲帽檐，设备进入训练；App 可显示连接，但不是开始按钮。

3. **30 秒：证明不是“头动了就算”**

   快速演示低头、转头、耸肩或短时动作，计数保持 0。

4. **70 秒：真实完成 3 次**

   每次保持 5 秒；App 显示进度，设备在完成时反馈，计数变为 1/3、2/3、3/3。

5. **30 秒：BLE 与米缸闭环**

   第三次后展示 `+¥5 入仓`，奶茶、潮玩、演唱会目标百分比同时更新。

6. **20 秒：离线能力**

   说明 App 不在前台时设备仍计数，重连使用序号去重同步。

7. **30 秒：可信度与未来**

   展示实测矩阵、已知限制，以及未来定制 PCB 小型化路线，不宣称医疗效果。

---

## 13. 交付物清单和仓库规则

最终仓库至少包含：

~~~text
PLAN.md
AGENTS.md
README.md
firmware/
  01_hardware_demo/
  02_pressure_imu_stream/
  03_recognition_ble/
app/
docs/
  product-contract.md
  ble-protocol.md
  recognition-rules.md
  test-results.md
cad/
  v0/
  release/
tools/
data/
  README.md
~~~

Git 节点：

- H2：`product-contract`
- H10：`first-ble-event`
- H24：`p0-demo`
- H42：`release-candidate`
- H48：`hackathon-release`

不上传：参与者原始 CSV、个人信息、Python 缓存、编译缓存、裸电池资料和本地 `硬件资料/` 目录。

---

## 14. 现在立刻执行的 60 分钟

### 0–15 分钟

- 指定主要演示者、演示手机和 App 平台。
- 你准备 ESP32、现有 IMU、RFP-602、电阻包、面包板、杜邦线和 USB；队友创建 App 项目。
- 测量演示者头围、左右宽、前后长，以及 ESP32、IMU、RFP-602、电池盒和 USB 插头实际尺寸。

### 15–30 分钟

- 保留已通过的 IMU 接线。
- 接 RFP-602 分压并只运行 ADC 输出，不同时改动作算法。
- 完成 60 秒静止数据和 20 次压力测试，先检查时间戳、丢样、基线和饱和。
- 队友用模拟 `rep/progress/snapshot` 消息建立训练页，不等待 BLE 实物。
- 开始画头环弧度、IMU 刚性座和压力座试片。

### 30–60 分钟

- 将 IMU 和 RFP-602 临时刚性固定，先采集 5–10 次标准 5 秒动作并立即画曲线；信号不重复就先修结构。
- 冻结 BLE UUID 和 JSON 字段。
- 完成头环/IMU/压力座尺寸测试片 CAD，提交打印队列。
- 将本小时产出提交 GitHub。

本小时结束必须回答四个问题：**100 Hz 数据是否完整、正确动作是否出现可重复信号、手机技术栈/协议是否冻结、帽架是否已有真实尺寸。**如果答案不是四个“是”，你不要进入 BLE 和整帽打印，队友只继续模拟数据 UI。

---

## 15. 最终完成定义

只有同时满足以下条件，才能说 48 小时产品已经落地：

- [ ] 真实 3D 打印帽架上装有真实工作的传感器、主控和清单内电池盒。
- [ ] 主控和焊点装入真实 3D 打印电子舱，不以效果图代替。
- [ ] 轻敲后可脱离 App 开始训练。
- [ ] 一次有效 chin tuck 必须保持 5 秒且只计一次。
- [ ] 完成 3 次后本地结束一组。
- [ ] 主要干扰动作达到固定测试门槛。
- [ ] BLE 将真实计数同步到实体手机 App。
- [ ] 断线期间不丢最终累计值，重连不重复奖励。
- [ ] App 正确更新总米缸和多个愿望目标百分比。
- [ ] 整机连续运行 30 分钟无崩溃、异常发热或结构松动。
- [ ] 固件、App、CAD、测试结果、安装说明和备份视频都已归档并推送。

少一项，就只能称为模块 Demo，不能称为完成的 Pecky Cap 产品样机。
